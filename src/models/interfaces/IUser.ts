import type {IAddress} from "@/models";
import type {ICompany} from "@/models";

export interface IUser {
  id: number;
  name: string;
  username: string;
  email: string;
  address: IAddress;
  phone: string;
  website: string;
  company: ICompany;
}
